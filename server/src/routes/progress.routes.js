import { Router } from "express";

const router = Router();

// Routes will be added in Phase 4
// router.route("/:courseId").get(verifyJWT, getProgress);
// router.route("/complete").post(verifyJWT, markVideoComplete);

export default router;
